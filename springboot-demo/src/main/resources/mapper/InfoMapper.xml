<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbootdemo.dao.InfoDao">

    <resultMap type="Info" id="Info">
        <result property="userId" column="userId"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
    </resultMap>

    <sql id="selectInfoVo">
        select userId, username, password from tb_info
    </sql>

    <select id="findOne" parameterType="Info" resultMap="Info">
        SELECT *
        FROM tb_info
        where username = #{username}
          and password = #{password}
    </select>

    <insert id="addOne" parameterType="Info">
        insert into tb_info
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">userId,</if>
            <if test="username != null and username != ''">username,</if>
            <if test="password != null and password != ''">password,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="username != null and username != ''">#{username},</if>
            <if test="password != null and password != ''">#{password},</if>
        </trim>
    </insert>

    <select id="checkReg" resultType="Info">
        select *
        from tb_info
        where username = #{username}
    </select>

    <update id="updateInfo" parameterType="Info">
        update tb_info
        <trim prefix="SET" suffixOverrides=",">
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if>
        </trim>
        where userId = #{userId}
    </update>


</mapper>